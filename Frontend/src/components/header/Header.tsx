import { Flex } from '@chakra-ui/react';
import React from 'react';

const Header: React.FC = () => {
    return (
        <Flex>
            {/* Content here */}
        </Flex>
    );
};

export default Header;